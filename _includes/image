{% for img in page.images %}
  <!-- {{ img.id }} v. {{ include.name }} -->
  {% if img.id == include.name %}
    <!-- found {{ img }} -->
    {% include image_tag.html image=img position=include.position %}
    {% break %}
  {% endif %}
{% endfor %}
