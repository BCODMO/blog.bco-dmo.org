{% for img_hash in page.images %}
  {% for img in img_hash %}
    {% if img == include.name %}
      <!-- {{ page.images[img] }} -->
      <!-- {{ img }} -->
      {% include image.html image=page.images[img] position=include.position %}
      {% break %}
    {% endif %}
  {% endfor %}
{% endfor %}
