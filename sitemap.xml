---
layout: none
---

<?xml version="1.0" encoding="UTF-8"?>
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">
  {% assign sitelastmod = nil %}
  {% for post in site.posts %}
    {% unless post.published == false %}
    <url>
      <loc>{{ site.url }}{{ post.url }}</loc>
      {% if post.sitemap.lastmod %}
        <lastmod>{{ post.sitemap.lastmod | date: "%Y-%m-%d" }}</lastmod>
      {% elsif post.date %}
        <lastmod>{{ post.date | date_to_xmlschema }}</lastmod>
      {% else %}
        <lastmod>{{ site.time | date_to_xmlschema }}</lastmod>
      {% endif %}
      {% if post.sitemap.changefreq %}
        <changefreq>{{ post.sitemap.changefreq }}</changefreq>
      {% else %}
        <changefreq>monthly</changefreq>
      {% endif %}
      {% if post.sitemap.priority %}
        <priority>{{ post.sitemap.priority }}</priority>
      {% else %}
        <priority>0.5</priority>
      {% endif %}
    </url>
      {% capture posttime %}{{post.date | date: '%s'}}{% endcapture %}
       <!-- posttime: {{ posttime }} -->
      {% if post.lastModified %}
        {% capture modtime %}{{ post.lastModified | date: '%s'}}{% endcapture %}
        <!-- modtime: {{ modtime }} -->
        {% if modtime > posttime %}
          {% assign posttime = modtime %}
        {% endif %}
      {% endif %}
      {% if sitelastmod is blank or posttime > sitelastmod %}
        <!-- mpdating sitelastmod: {{ posttime }} -->
        {% assign sitelastmod = posttime %}
      {% endif %}
    {% endunless %}
  {% endfor %}
  <url>
    <loc>{{ site.url }}</loc>
    <lastmod>{{ sitelastmod | date_to_xmlschema }}</lastmod>
    <changefreq>monthly</changefreq>
    <priority>1</priority>
  </url>
</urlset>
